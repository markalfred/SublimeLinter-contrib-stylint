language: python
python:
  - "3.4"
# command to install dependencies
install:
  - pip install flake8
  - pip install pydocstyle
# command to run tests
script:
  - flake8
  - pydocstyle
  - python -m unittest discover
